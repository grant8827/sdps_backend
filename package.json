{
  "name": "backend",
  "version": "0.1.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "dev": "node --env-file-if-exists=.env node_modules/nodemon/bin/nodemon.js index.js",
    "test": "node --env-file-if-exists=.env --test --test-isolation=none"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
